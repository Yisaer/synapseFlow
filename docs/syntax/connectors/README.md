# Connector String Syntax

This directory documents template syntax evaluated for connector
configuration, separately from SQL and per-row encoder templates.

- `property_templates.md`: static `prop("key")` interpolation.
