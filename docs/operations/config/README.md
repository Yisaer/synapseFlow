# Configuration Operations

This directory contains deployment configuration notes and override behavior.

Related topics:

- `env_overrides.md`: whitelist-based environment variable override rules.
- `static_properties.md`: process-wide static properties and their security
  boundary.
- `../logging/README.md`: process-global logging configuration and backend
  selection.
